{
  "name": "doge-my-site",
  "version": "0.9.1",
  "description": "Add 'MUCH WOW' to any page on the web.",
  "main": "dist/index.js",
  "scripts": {
    "browserify": "browserify dist/index.js -o dist/index.js",
    "build": "tsc && cp -a ./assets/. ./dist && npm run browserify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Solati mkslt04@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "0.0.59",
    "browserify": "^16.1.0",
    "typescript": "^2.7.2"
  },
  "dependencies": {}
}
