{
  "name": "doge-my-site",
  "version": "0.9.0",
  "description": "Add 'MUCH WOW' to any page on the web.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp -a ./assets/. ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Solati mkslt04@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "0.0.59",
    "typescript": "^2.7.2"
  }
}
